# Security Note: This file contains example values only
# All sensitive credentials should be stored in Supabase Secrets
NODE_ENV=development
PORT=3000
LOG_LEVEL=info
# Public redirect URI - this is safe to store in code
GOOGLE_REDIRECT_URI=http://localhost:8080/callback/google
